[build]
builder = "nixpacks"

[deploy]
startCommand = "node daily-scheduler-2am.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
NODE_ENV = "production"
TZ = "Asia/Kolkata"